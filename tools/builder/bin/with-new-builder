#!/usr/bin/env bash

here=$(dirname "$0")
source "$here/../lib/bash/prelude.bash"

[[ -n "$*" ]] || die "usage: $0 <cmdline ...>"

builder=$("$__HERE/mkbuilder")
[[ -z ${KEEP_BUILDER:-} ]] && trap "rm -rf $builder" EXIT

export ASPIREBUILD=$builder/.aspirebuild

cd "$builder"
"$@"
